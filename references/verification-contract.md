# 开发验证范围

测试验证接口、参数、数据转换、部分成功和文档一致性，不构建布局评分或运行期审批。模型决定设计和分析时机。

MCP 回归覆盖常见操作、任意角度、批量输入、当前端点、镜像、未知数据和传输失败。Skill 测试覆盖相对引用、MCP 优先路由、无强制调用链、工程知识保留和示例类型。静态测试不证明模型未来每次都能画好 PCB。

真实客户端只读、模拟写入与真实板写入分别报告。工具或 Skill 维护不在生产板上写测试对象。真实写入需要明确的测试副本范围，自动化测试通过不冒充实机编辑通过。

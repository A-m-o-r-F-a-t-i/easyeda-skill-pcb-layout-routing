---
name: easyeda-pcb-layout-routing
description: 嘉立创 EasyEDA PCB 布局、显式布线、铺铜、丝印和制造导出。优先用 PCB MCP 简化原生 API 操作，读取元件封装与引脚网络朝向总览，批量编辑并利用整体/局部 SVG 反馈。AI 自主设计和选择分析时机。
version: 6.0.0
---

# PCB 设计与 MCP 操作

## 职责与输入

用户提供已画好的原理图并要求 PCB，即采用 `approved_schematic`。现有器件、封装、针序和参数是输入，不重新开展选型、逐脚电气审查、手册搜集或高度/气隙调查，也不把这些事项延后为交付待办。读取完整元件和焊盘信息是为了安排空间与连接。

AI 决定功能分区、位置、旋转、板面、完整路径、铜区与下一步动作。Skill 提供方法和工作流建议。MCP 封装原生调用、对象解析、单位转换、批量执行和事实反馈，不作布局许可或设计评分，不自动布局、自动寻路或自动布线。

**优先使用 MCP**：常见操作直接调用对应 PCB MCP。不要为移动、走线、过孔、板框、铜区、文字、导出重新输出原生 API 脚本。确有未封装能力或实现故障时才使用 `easyeda-api` 定位具体缺口，补进 MCP 后恢复简化接口；不要求人为制造一次失败调用。

## 取得设计信息

加载插件后按意图发现工具。`pcb_status` 取得目标和单位，`pcb_read(kind="overview")` 返回整板组件、封装、尺寸来源、实际焊盘、网络关联、当前朝向和常用旋转后的引脚方位。数据已在上下文中时复用；需要局部时用 refs、region 或具体对象读取。

唯一连接中的 PCB 可省略 target。存在多个候选时用返回的文档 UUID 作为简短 target，之后继续使用已确定目标，不能随用户临时查看另一块板而改变施工对象。编辑器内部坐标是 mil，简化编辑和总览默认 mm，读取原生对象时以响应的 units 为准。

本体、装配外形、丝印和焊盘包络分别表达，缺失保持未知，不把包围盒当实体。朝向图由实际焊盘计算，底面保持真实镜像关系；同面旋转预测不等同于跨面预测，翻面后使用返回的新焊盘。详见 [数据与视图](references/data-feedback.md)。

## 一次表达完整动作

`pcb_execute_plan` 直接接受 operations。无需 mode、prepare、guard、expected、批次上限或旧对象。只填写要改的字段，未填写字段保持原值。支持位号、`U1.12` 引脚端点、层名、完整路径和位置数组。

可一次布局整板或多个模块，也可一次提交大量路线。规模由当前设计决定；MCP 内部处理传输分片和实际返回。整体平移、对齐、等距和径向排列是执行明确几何，不替 AI 选择位置。普通编辑无需先写计划文件，大型可复用输入才用 planPath。

示例、操作族和参数见 [操作速查](references/operation-recipes.md)。需要特定操作的完整类型时读取 `pcb_read(kind="operations")`，不查底层源码猜字段。

## 布局与布线经验

- 结合网络链路与器件服务对象分组。去耦围绕实际电源脚和返回，反馈围绕取样点与反馈端，ADC 输入串阻/电容围绕 ADC 端，自举与栅极外围跟随驱动回路。相同网络名不表示所有电容应堆在一起。
- 接口按用户要求安排。贴片外部接口通常靠板边、开口朝外，圆板可用任意角度顺应板边。原点方便计算即可，MCP 不限制圆心或多边形顶点必须在原点。板面、机械尺寸、接口区域只在本任务中生效。
- 先考虑关键连接、整组出入口、扇出、主电流与返回空间。密脚器件朝向服务于连接，避免外围堵住出口。可直接形成大范围布局，再根据整体反馈调整，不要求登记功能组或多方案评分。
- 线路由 AI 明确给出端点、层、宽度和节点。常规区域可优先 45°，倾斜焊盘和径向接口按实际方向引出。用户禁止直角时由设计遵守，MCP 不扩大解释成只允许三种方向。
- 大电流的连续开放区域优先用局部宽铜或铺铜，留意焊盘出口、最窄铜颈和换层孔阵列。宽线适合短颈和狭窄通道；高 dv/dt 开关节点控制铜面积，供电去程与返回一起安排。
- 保持实际参考铜连续，换层时考虑回流。SMD 焊盘在另一层的投影重合不构成连接。通道被堵、线路反复绕行时可回调布局、拆除明确旧铜后重布，不通过改网或放宽既定规则掩盖问题。

更多方法见 [布局与布线](references/placement-routing-closure.md)、[FOC 回路](references/experimental/motor-foc.md)。这些是设计提示，执行顺序可合并、跳过或交错。

## 观察反馈与按需分析

编辑默认返回实际完成、失败和未执行内容，以及局部或整板 SVG。用 view=auto/local/board/none 选择反馈；连续编辑可关闭自动视图。单独 SVG 支持 top/bottom/both、区域、图层和网络高亮。局部图可显示引脚号/网络名；SVG 标注只是观察叠加，不是 PCB 上的丝印。

AI 依据数据和图形决定继续、调整或分析。DRC、几何、网表、丝印工具按需调用，不是每次编辑的前置条件，也不决定编辑权限。布局成形、关键连接变化、重铺后或收尾通常值得运行相应分析，具体时机由任务需要决定。已经取得的有效结果可以复用。

原生 DRC 的 RUNNING 使用同一 drcJobId、save=false 继续读取；完整总数与明细分页分开。失败、未知或空页不能报告零违规。可信封装内部项、同网间距和施工期未连接结合实际对象判断，不能用这些理由掩盖真实跨元件冲突。详见 [DRC 数据判读](references/drc-policy.md)。

## 人工介入、失败与收尾

保留最新人工位置、角度、板面和有效文字，只提交本次要改的字段。批量移动已有布线元件时，由 AI 安排相关旧铜和新端点，MCP 不隐式重布或整网删除。明确暂停后不再发起相关新写入。

部分成功或响应丢失时先利用实际回执和必要局部读取，保留已完成对象，仅提交明确未完成的动作。不能盲目重放原批次，也不能把 unknown 当作未写入。目标变化和原生错误如实处理，明确权限拒绝不换入口绕过。

完整 PCB 任务包含功能丝印、必要分析、保存和约定生产文件。用 pcb_cleanup_components 按需解锁或隐藏位号显示，保留 Designator 身份与 BOM 关联；未显式修改的锁定状态由 MCP 保留。真实接口丝印与 SVG 辅助标识分开，见 [丝印](references/silkscreen-usability.md)。

生产输出优先用 pcb_export 单项或文件组合，实际生成后报告路径、来源和仍存在的问题。未完成的连接、实际规则错误或文件缺失应说明；不追加原理图上游复审。仅维护工具或 Skill 时，不为验收改动正在工作的 PCB。

## 按需资料

[工具索引](references/tool-index.md) · [操作速查](references/operation-recipes.md) · [数据与视图](references/data-feedback.md) · [布局布线](references/placement-routing-closure.md) · [DRC](references/drc-policy.md) · [客户端数据差异](references/client-compatibility.md) · [丝印](references/silkscreen-usability.md) · [专项范围](references/experimental/index.md)

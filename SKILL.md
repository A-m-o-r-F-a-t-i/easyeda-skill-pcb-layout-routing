---
name: easyeda-pcb-layout-routing
description: 将已确认原理图直接落实为 EasyEDA PCB，或继续现有板的布局、显式布线、铺铜和丝印。信任既有选型、连接和封装，只读取作图所需对象与焊盘网络坐标；按供电回路、信号链和板面要求执行，不重新审查电路、不补查器件手册，并在布局、布线、铺铜和交付节点运行原生 DRC。
version: 5.4.2
---

# PCB 执行：按确定原理图画板

## 先记住这七条

1. **原理图是输入，不是审查对象。** 用户提供已完成原理图并要求画 PCB，即采用 `approved_schematic`，无需再问是否确认。沿用其器件、数值、封装、引脚分配、针序、极性、NC 和网络，不复核选型、电气性能或外围是否正确，也不把这些检查推迟到交付前补做。
2. **读数据是为了画线，不是找电路问题。** 只取下一批要移动的对象旧值、尺寸轮廓、焊盘坐标及所属网络，或本模块所需的供电/信号连接关系。已有数据复用；不逐颗翻属性、不遍历 NC、不导出 BOM 做审计、不因怀疑引脚或容值正确性查数据手册。
3. **布局是可修改的草案。** 按给定面别、板框、孔位和接口方向直接放置，普通元件必须保持未锁定；`component.modify` 未明确要求锁定时写回 `primitiveLock:false`，不能继承历史误锁。只有用户明确指定的固定对象才可显式锁定。缺少气隙、磁钢、封装高度、三维模型、热或额定值资料，不新增调查、待确认表或放行阻断。
4. **工作重点是网络与回路。** 先安排供电去程和返回、开关回路、去耦、自举、Kelvin 采样和接口信号链，再做普通连接。难走就旋转或移动局部模块，不为整齐牺牲铜通道。
5. **先规划整组通道，再逐根落线。** 同一区域往返的相关连接先按供电/回流、开关/大电流、采样/反馈、接口/控制和普通信号分组，确定入口、出口、瓶颈、层、参考/回流、相对次序和预留空间；单根最短路径不能抢占整组唯一出口。通道规划只组织空间，不改变网络、针序或既有约束。
6. **DRC 是阶段门，不是逐步仪式。** 普通保存使用 `runDrc:false`；布局成形且开始正式布线前、关键布线或整板布线完成后、重建实际铜后、最终导出前必须运行 `runDrc:true`。重大板框、孔槽、禁区、层面或大范围布局变化会使相关阶段结果失效。单颗移动、单根走线或每一小批写入后不重复整板 DRC，也不修改用户的编辑器实时 DRC 设置。
7. **拿到下一动作的数据就执行。** 生成明确计划，`prepare → execute`，消费工具自带读回后继续。不要重复 validate、prepare、pinmap 或检查点仪式；实际写入失败才做精确对账。只报告真实板上变化。

## 职责边界

本 Skill 负责板框、布局、走线、过孔、铺铜、丝印、PCB 制造检查和生产文件，不负责原理图设计审查、参数重新论证或样机性能认证。用户明确要求电路审查时才切换到原理图 Skill；不会因 FOC、功率器件、陌生型号或空网络自动触发审查。

沿用已给定的工程尺寸和布线规则，不虚构额定电流、温升或编码器精度。用户明确的板框、孔位、禁区、装配面、接口朝外和主体不越界仍应落实；不为复证气隙、高度等上游输入上网查资料。自然遇到确定的源数据导入缺失，仅指出具体对象并继续其他区域，不主动搜寻可疑电路问题，不擅自改网或改料。

## 一条执行路线

| 当前工作 | 直接动作 | 需要读取的最少数据 |
| --- | --- | --- |
| 接手 PCB | 锁定唯一目标；空白板导入已关联原理图，已有 PCB 接着画 | 文档身份、单位、现场机械对象和本次要改的对象 |
| 建板框 | 按给定尺寸创建板框、安装孔、槽孔、禁区和层分工 | 已存在的同类对象，避免重复创建 |
| 放模块 | 放接口、固定传感器、功率/电源、主控及其外围；布局成形后做 `LAYOUT_DRC` | 本模块对象旧值、封装轮廓、连接关系 |
| 走线 | 先按网络组规划通道和功率铜骨架，再落关键回路及剩余网络；到关键/整板布线节点做 `ROUTING_DRC` | 对应网络组的焊盘坐标/层/网络、附近障碍和现有规则 |
| 收尾 | 批量解锁普通组件并清除全部组件位号丝印显示，再重建铺铜、完善回流、功能丝印及测试点；做 `POUR_DRC` | 本次改变的组件锁定、铜区、网络和文字 |
| 交付 | 做 `FINAL_DRC`、连接与网表同步检查，修正实际 PCB 问题，保存导出 | 最终 PCB 和生产文件 |

首次确实需要整板连接索引时，可一次获取紧凑的器件/焊盘网络表并缓存，按实际连接关系形成网络组，用于后续通道规划和布线；不展开成电气预检。后续只更新自己或用户改变的对象，不因一次元件移动或 changeEpoch 增长重读全板。已有同步证据直接用，只有首次导入或实际原理图变更才走 ECO；不在每次续作时重新比较网表。

旧任务的 S0～S8 编号可以保留：S0 目标、S1 导入/同步、S2 机械、S3 布局、S4 关键布线、S5 剩余布线、S6 铺铜丝印、S7 最终 PCB 检查、S8 导出。它们只是进度标签，不是九组前置审批。

## DRC 阶段与判定

必须使用原生 `pcb_save_and_drc(save=true, runDrc=true)`完成 `LAYOUT_DRC`、`ROUTING_DRC`、`POUR_DRC` 和 `FINAL_DRC`。相邻里程碑处于同一未变化板状态时可由一次检查同时满足，但不能让整板只剩最终检查。DRC 验证当前 PCB 的制造与连接，不触发数据手册、额定值、引脚定义、器件选型或外围参数复审。

只有回执同时满足 `drcState="COMPLETED"`、`drcVerified=true`，并给出 `drcErrorCount` 与 `drcSummary`，才算取得本次原生 DRC 结果。`RUNNING` 必须用同一 `drcJobId`、`save=false` 继续读取；超时、失败、作业丢失或缺少明细均为 `UNVERIFIED`。分页中的空页不代表零违规，阶段判定始终使用完整总数与汇总，`drcItemsHasMore=true` 时按需续取明细。

判定顺序以物理问题优先：跨组件焊盘、封装实体或装配外形重叠在布局阶段必须立即修正，即使同网络也不得豁免；确认不存在物理重叠后，同网络焊盘间距报告可记为非阻断；同一未修改且来源可信的嘉立创/EasyEDA 库封装内部报告可记为非阻断；未布线报告只在布线尚未完成时非阻断，`FINAL_DRC` 必须清零。归属、网络或规则含义不明时先检查，不能自动忽略。

“忽略”只表示保留原始 DRC 项并记录豁免原因，不等于关闭或放宽规则、隐藏或删除对象、修改网络，或报告零错误。阶段结果至少分为 `blocking`、`waived` 和 `unresolved`；只有 `blocking=0` 且 `unresolved=0` 才能通过。详细规则见 [DRC 策略](references/drc-policy.md)。

## 布局与回路知识

- **供电**：入口→保护→储能→转换→负载，同时安排返回路径。开关电源的芯片、输入高频电容、续流器件和电感紧凑布局，反馈从输出侧返回；不重算已有反馈阻容或使能逻辑。
- **去耦**：按原理图分区和连接关系放到服务芯片的电源脚旁，电源和地连接一起短。同一电源网络不代表电容可随便互换位置；无需再次检查容值是否符合手册。
- **FOC**：每相上下管、开关节点、本地母线旁路和相线出口形成局部功率单元。自举电容连接原网表中的 BST 与开关参考端并靠近驱动器；栅极去程和源极返回共同短。详见 [FOC 布局速查](references/experimental/motor-foc.md)，其中不包含选型、死区或采样时序审查。
- **采样**：先从现有连接关系区分主电流路径与测量支路，不能仅按 `I_SENSE` 名称用细线。采样电阻的负载电流走宽铜，放大器输入从对应焊盘独立成对引出 Kelvin 支路；不改采样拓扑、增益或滤波参数。
- **主控与接口**：旋转主控以利 PWM、ADC、SPI 和外部接口扇出，去耦不堵整排焊盘。接口→保护→串阻/收发器→主控按原信号链摆放，串联器件两侧不同网络仍属同一链路。
- **板面与边缘**：按用户定义的主电路面放完整模块，不因另一面空白把主电路搬过去。贴片外部接口默认放板边、真实插口朝外；用现有封装图形判断开口，不把 `rotation=0` 当成统一朝向。圆板按局部径向确定朝外，保留螺钉和线束通道。
- **整组通道**：相关线路在拉第一根前先确定共用走廊的入口、出口、瓶颈、层、参考/回流和自然顺序。直线段至少容纳各线宽、相邻间距和两侧退让，拐角、扇出、过孔及铜区转换另留余量；不让先布的一根线占满后续网络的唯一出口。
- **局部闭环**：先用真实规则试布最受约束的网络组。若后续只能钻缝、反复折返、沿板边绕行、堆积过孔、挤出铜颈或切断参考面，停止给最后一根线打补丁，重分该组通道或移动/旋转可移动器件。每组完成后只回看该组、邻域与剩余出口，不触发整板 DRC/pinmap；达到 `ROUTING_DRC` 里程碑时必须运行原生检查。
- **铜与回流**：至少保留连续的主要 GND 参考；换层点附近安排与实际参考层相连的地回流，不按层名假定参考铜连续。电源主干、焊盘出口、最窄铜颈和过孔阵列按现有电流等级/布线规则处理，不缩规则强行连通，不承诺未经验证的整板载流性能。
- **可铺铜区域**：两个大焊盘、功率器件或功率模块之间存在连续可用的矩形、梯形、扇形等区域时，直接创建绑定对应网络的局部实心铜区，不用一根长距离宽线跨过可铺铜区域。
- **开放区大电流**：空间宽裕且不会割裂参考面、阻断回流路径或扩大高 `di/dt` 回路时，大电流部分全部使用连续铺铜；宽线只用于焊盘短颈、狭窄通道、铜区之间的短过渡和过孔阵列收束。
- **开关节点边界**：高 `dv/dt` 开关节点只保留实现连接所需的铜面积，不因“大电流优先铺铜”向反馈、晶振、基准或敏感采样区域扩张。
- **细节**：禁止整板自动布局、自动寻路和自动布线。使用斜线/圆弧避免直角；不对所有接口统一等长，不加无意义蛇形。完整 PCB 任务默认清除全部组件位号丝印显示：对挂在组件上的 `Designator` 同时写入 `keyVisible:false` 与 `valueVisible:false`，不能删除或改写其身份属性；组件位号、父对象和 BOM 关联保持不变。接口功能、针脚网络、极性、方向及用户有意义文字必须保留。保留用户的孔槽、独立接线焊盘和人工改动。

## 常用工具，不必到处查

| 意图 | 优先工具与用法 |
| --- | --- |
| 已知目标 | `pcb_status`；只在目标未知时 `pcb_list_targets` |
| 读取本批 | `pcb_read(kind="components", ids=[...])`；属性只取必要对象；`pcb_inspect_pinmap(componentIds=[...])`用于取得作图端点，不用于复审芯片 |
| 移动/翻面/布线 | `pcb_execute_plan`；操作用 `component.modify`、`route.create`、`via.create` |
| 解锁/删位号 | `pcb_cleanup_components`；先 `mode="preflight"`，再以原 guard 执行 `mode="execute"`；默认 `unlockComponents=true, deleteReferenceDesignators=true` |
| 孔槽/接线端 | `outline.create`、`hole.create`、`pad.create`；已有独立对象不重复创建 |
| 铺铜/丝印 | `pour.create` 后 `pcb_rebuild_pours`；文字用 `pcb_execute_text_plan` |
| 普通保存 | `pcb_save_and_drc(save=true, runDrc=false)` |
| 阶段 DRC | 到四个里程碑调用 `pcb_save_and_drc(save=true, runDrc=true)`；若返回 `RUNNING`，沿用 `drcJobId` 续取至 `COMPLETED`，再按完整汇总分类 |
| 最终检查/导出 | `FINAL_DRC`通过后 `pcb_export(kind="backup"/"gerber"/"bom"/"pick_place", ...)` |

每次原理图首次导入/ECO 后，以及最终丝印收尾前，必须执行 `pcb_cleanup_components`：清除全部挂在真实组件上的位号丝印显示，并在没有用户明确锁定例外时解锁全部组件。真实客户端会保留 `Designator` 作为组件身份，工具只把其 `keyVisible/valueVisible` 同时设为 `false`，不得删除属性本体或改写 value。执行后必须确认 `allComponentsUnlocked`、`allComponentDesignatorSilkscreenRemoved`、`componentDesignatorIdentityPreserved`、`independentStringsUnchanged`、`nonDesignatorAttributesUnchanged` 和 `componentIdentityAndGeometryUnchanged`；预检后状态变化就重新预检，不能复用旧 guard。具体可复用字段与示例见 [操作速查](references/operation-recipes.md)。首次使用某工具时读一次当前 Schema，后续复用；参数变化或真实契约错误才重新查询。工具的目标/旧状态保护保留，模型不重复实现它的校验器。

## 写入和恢复

输入显式指定 mil 或 mm，1 mil = 0.0254 mm。API 读回是作图坐标；底层的旋转与镜像交给元件翻面接口。移动后使用工具读回的新焊盘坐标，缺少坐标时只读取本批焊盘，不能手算镜像猜端点。

计划绑定精确目标，`expected`来自当前对象，`prepare`返回的 guard 原样用于未改动计划的 `execute`。普通元件只改变位置、角度和层，不改网络且默认解锁；除非用户明确要求，不能在 `set` 中写 `primitiveLock:true`。未布铜用 `copperPolicy:"unrouted"`；已有铜用 `"replan"`并处理受影响旧铜。最多 24 个展开操作一批，成功后用工具独立读回继续，不在每一批后额外跑全板 pinmap/DRC；达到阶段门时必须运行。

工具结果未知时只按 `recoveryDirective` 读取精确对象，保留已确认前缀，只执行未完成后缀，不能盲目重放。连接错误最多两次轻量探测和一轮定向修复；无有效连接就如实报告，不以换 PCB 区域假装能够绕过全局断联。明确权限拒绝不换入口绕过。

仅在模块完成、故障或切换任务时保存简短断点：目标、阶段、已改对象、下一动作和真实阻断。不逐次保存复杂续作帧，不维护重复审批表。修工具只修当前缺陷、验证后回到具体板上动作；纯 Skill 修改只做文档/示例验证，不为验收 Skill 改动正在工作的 PCB。

## 最终 PCB 检查

完成布线、铺铜和丝印后，检查未连接/错误铜连接、原理图导入一致性、`FINAL_DRC` 分类结果、供电回路与铜颈、已给定几何约束、接口朝向、面别、普通组件未锁定、组件位号丝印显示已清空且 `Designator` 身份仍保留、功能丝印和导出对应关系。网表比较只确认复制/导入一致，不重新论证源电路针序是否合理。工具明确报告的实际短路、跨组件重叠和制造错误必须修复，不通过改网、隐藏对象或放宽规则清错；允许豁免项必须保留原始报告和理由。

`READY_FOR_FAB`只表示本任务范围的 PCB 制造文件及必要 PCB 检查通过；`READY_FOR_ASSEMBLY`还要求 BOM、坐标、面别和装配方向与当前工程一致。它们不等于电路选型、磁气隙、器件高度实测或样机性能认证。真正未完成的 PCB 或文件问题报告 `NOT_READY`，不把本任务未委托的电路审查添加为条件。

## 按需资料

默认只需本页，遇到具体问题再读对应资料：阶段判定 [DRC 策略](references/drc-policy.md)，常用字段 [操作速查](references/operation-recipes.md)，PCB 流程 [工作模式](references/workflow-patterns.md)，整组通道与局部重排 [布局布线](references/placement-routing-closure.md)，接口 [物理端点](references/product-physical-interfaces.md)，最终输出 [检查导出](references/inspection-and-release.md)。完整工具/兼容实现留在技术参考，普通画板不加载源码和全套测试。

## 环境变量

本 Skill 不需要环境变量。连接和文件路径由宿主提供，任务数据存入任务工作区而非 Skill 包。

# 已确认原理图到 PCB

原理图、元件、封装与针序已经确定。请采用 approved_schematic，按以下本板要求完成布局、显式布线、铺铜、丝印、必要分析与生产文件。

优先通过 MCP 读取元件封装、尺寸来源、朝向、引脚网络整体数据。由你决定功能区、位置、角度和完整线路，可以一次操作大量元件与线路。MCP 负责简化原生 API 调用，常见动作不要另写 API 脚本。

使用整体或局部 SVG 观察结果，尤其关注引脚出口、实际连接、主电流与返回以及接口方向。DRC 和其他分析由你在能帮助判断时调用，不要求逐步审批或固定小批。困难区域可调整布局并处理明确相关旧铜。

保留最新人工修改和任务范围，部分成功先读取实际结果，不盲目重放。缺少某个 MCP 常见操作时补充封装，不把原生 API 当常规绕行路径。

以下填写本板尺寸、孔槽、板面、接口、叠层、线宽与交付要求。这些是本任务要求，不写入通用 Skill。
